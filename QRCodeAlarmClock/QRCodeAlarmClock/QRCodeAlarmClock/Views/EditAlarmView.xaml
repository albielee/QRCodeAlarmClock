<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:QRCodeAlarmClock.ViewModels"
             xmlns:views="clr-namespace:QRCodeAlarmClock.Views"
             x:Class="QRCodeAlarmClock.Views.EditAlarmView"
             x:Name="this">

  <ContentView.Content>
        <Grid x:Name="mainGrid"
              BindingContext="{Binding BindingContext, Source={x:Reference this}}">
            <Frame CornerRadius="20"
                   x:Name="editorViewFrame"
                   Padding="0,20"
                   BackgroundColor="{StaticResource DarkGrey}">
                <Grid x:Name="AlarmEditingViews">
                    
                </Grid>
            </Frame>
        </Grid>
    </ContentView.Content>
</ContentView>